name: Run Unit Tests

on: push

jobs:
  test:
  
    runs-on: [ubuntu-latest, windows-latest]

    steps:
    - name: Run unit tests
      run: nmp test
    - name: Upload test results
      uses: actions/upload-artifact@v2
      with:
        name: test-results
        path: test-results
      if: always()
